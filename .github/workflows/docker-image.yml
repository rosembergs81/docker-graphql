name: Docker Graphql Image

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest
    environment: DOCKER

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Git Semantic Version
      uses: PaulHatch/semantic-version@v4.0.3
      with:
       major_pattern: "major:"
       minor_pattern: "feat:"
       format: "${major}.${minor}.${patch}-prerelease${increment}"
      id: version

    - name: Docker Login
      env:
        DOCKER_USER: ${{ secrets.DOCKER_USER }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: |
        echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USER" --password-stdin

    - name: Build Docker Image
      env:
        NEW_VERSION: ${{ steps.version.outputs.version }}
      run: |
        docker build -t rosembergs81/docker-graphql:$NEW_VERSION .
        docker build -t rosembergs81/docker-graphql:latest .
        
    - name: Push Docker Imag
      env:
        NEW_VERSION: ${{ steps.version.outputs.version }}
      run: |
        docker push rosembergs81/docker-graphql:$NEW_VERSION
        docker push rosembergs81/docker-graphql:latest


